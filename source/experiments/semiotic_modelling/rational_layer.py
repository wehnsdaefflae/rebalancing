from typing import Optional

from source.experiments.semiotic_modelling.content import SHAPE_A, ACTION, STATE


def rational_layer(level: int, model, state: STATE, action: Optional[ACTION], consequence: SHAPE_A, sig: float = .1, alp: float = 1., h: int = 1):
    pass